## Flow Automation Studio 2.8.0

**Tự cập nhật (Windows và macOS).** Từ bản này, app tự hỏi GitHub có bản mới không (4 giờ một lần), tự tải về chạy ngầm, kiểm mã băm, rồi cài khi bạn bấm "Cài & mở lại" hoặc khi bạn thoát app. Không bao giờ cài giữa lúc đang chạy mẻ. Tài khoản, phiên đăng nhập Google, cài đặt và dự án giữ nguyên.

**Model video** (Cài đặt → 🎚 Model video):
- Chọn sẵn model cho cả mẻ, khỏi phải chỉnh tay trên Flow.
- **Tự chuyển khi "Veo 3.1 - Lite [Lower Priority]" bị chặn:** app ngừng gửi Lower Priority của tài khoản đó một thời gian, chuyển sang model dự phòng bạn chọn (tốn credit) và chạy tiếp ngay. Hết giờ thì thử lại Lower Priority.
- Xen kẽ hai model trong một mẻ (ví dụ 1 Lower Priority, 1 Lite) — để chia chi phí.
- Nút "Đọc danh sách model từ tab Flow đang mở" để thử nhanh.

Để trống mục Model video thì app chạy y như bản cũ.

**Lưu ý:** Windows nên dùng bản cài đặt (`-setup.exe`) — bản portable không tự thay được chính nó. Mac nên kéo app vào thư mục Applications.
